import React from 'react';
import ReactDOM from 'react-dom/client';
import IronRealm from './iron-realm';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IronRealm />
  </React.StrictMode>
);
